[package]
name = "base64"
sources = ["sp_base64.c"]   # carried C is declared, not discovered (#4362)

# Pre-installed spin package: ships with the compiler, versioned with it.
# A typed native binding carrying its own C (sp_base64.c).
